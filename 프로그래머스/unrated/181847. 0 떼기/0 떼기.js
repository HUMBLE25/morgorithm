function solution(n_str) {
    // let answer = n_str;
    // for(const id in n_str){
    //     if( ! +n_str[id] ){
    //          answer = answer.slice(1);
    //     }else{
    //         return answer;
    //     }
    // }
    return + n_str + '';
}